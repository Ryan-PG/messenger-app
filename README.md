# Client Server Python messager
### Socket Programming


##### For Using:
Enter this command in cmd/bash:
``` 
python server.py
```
> This command runs the server.

You can use multiple clients. just use this command to run clients, each in one cmd/bash:
```
pythone client.py
```